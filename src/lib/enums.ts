export enum Column {
    Left = 'left',
    Middle = 'middle',
    Right = 'right'
};

export enum UserPermission {
    Administrator = "administrator",
    EditLayout = "edit_layout",
    UploadFiles = "upload_files"
}
